{
  "name": "soichih.github.io",
  "version": "1.0.0",
  "description": "``` sudo apt install ruby-bundler ruby-dev sudo gem install ffi -v '1.9.18' bundle install ```",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bundle exec jekyll build",
    "dev": "bundle exec jekyll serve"
  },
  "author": "Soichi Hayashi"
}
